name: MiniMax Assistant

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  issues:
    types: [opened, assigned, labeled]
  pull_request_review:
    types: [submitted]

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  minimax-response:
    if: |
      (github.event_name == 'issue_comment' &&
        contains(github.event.comment.body, '@minimax') &&
        (github.event.comment.author_association == 'OWNER' ||
         github.event.comment.author_association == 'MEMBER' ||
         github.event.comment.author_association == 'COLLABORATOR')) ||
      (github.event_name == 'pull_request_review_comment' &&
        contains(github.event.comment.body, '@minimax') &&
        (github.event.comment.author_association == 'OWNER' ||
         github.event.comment.author_association == 'MEMBER' ||
         github.event.comment.author_association == 'COLLABORATOR')) ||
      (github.event_name == 'pull_request_review' &&
        contains(github.event.review.body, '@minimax') &&
        (github.event.review.author_association == 'OWNER' ||
         github.event.review.author_association == 'MEMBER' ||
         github.event.review.author_association == 'COLLABORATOR')) ||
      (github.event_name == 'issues' &&
        (contains(github.event.issue.body, '@minimax') || contains(github.event.issue.labels.*.name, 'minimax')) &&
        (github.event.issue.author_association == 'OWNER' ||
         github.event.issue.author_association == 'MEMBER' ||
         github.event.issue.author_association == 'COLLABORATOR'))

    runs-on: ubuntu-latest
    env:
      CLAUDE_CODE_MODEL_NAME: ${{ vars.MINIMAX_CODING_PLAN_MODEL_NAME || 'MiniMax-M2.1' }}
    steps:
      - uses: actions/checkout@v4

      - name: Check MiniMax API key configured
        env:
          MINIMAX_API_KEY: ${{ secrets.MINIMAX_CODING_PLAN_API_KEY }}
        run: |
          if [ -z "$MINIMAX_API_KEY" ]; then
            echo "Missing or empty secret: MINIMAX_CODING_PLAN_API_KEY"
            exit 1
          fi

      - name: Run Claude Code with MiniMax Coding Plan
        uses: anthropics/claude-code-action@v1
        env:
          ANTHROPIC_BASE_URL: "https://api.minimax.ai/api/anthropic"
          API_TIMEOUT_MS: "3000000"
          CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC: "1"
          ANTHROPIC_MODEL: ${{ env.CLAUDE_CODE_MODEL_NAME }}
          ANTHROPIC_DEFAULT_SONNET_MODEL: ${{ env.CLAUDE_CODE_MODEL_NAME }}
          ANTHROPIC_DEFAULT_OPUS_MODEL: ${{ env.CLAUDE_CODE_MODEL_NAME }}
          ANTHROPIC_DEFAULT_HAIKU_MODEL: ${{ env.CLAUDE_CODE_MODEL_NAME }}
          CLAUDE_CODE_SUBAGENT_MODEL: ${{ env.CLAUDE_CODE_MODEL_NAME }}
        with:
          anthropic_api_key: ${{ secrets.MINIMAX_CODING_PLAN_API_KEY }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          trigger_phrase: "@minimax"
